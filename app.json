{
	"name": "Reverse Proxy",
	"description": "Spin up a reverse proxy quickly on Heroku",
	"keywords": [
		"reverse proxy",
		"nginx"
	],
	"repository": "https://github.com/picelboi/heroku-reverse-proxy",
	"buildpacks": [
		{
			"url": "http://github.com/picelboi/heroku-buildpack-nginx.git"
		}
	],
	"env": {
		"UPSTREAM_SERVER": {
			"description": "URL of upstream server to which all requests should be proxied"
		}
	},
	"stack": "heroku-24"
}
